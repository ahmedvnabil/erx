# معمارية Egypt Research Commons

## الهدف

منصة بحث عامة للشأن المصري تفصل بين جمع المادة، حفظ النسخ، بناء المعرفة،
والعرض للباحثين والوكلاء. الرابط الأصلي وتاريخ الأرشفة جزء إلزامي من كل نتيجة.

## مسار البيانات

```text
المصادر العامة
  │ RSS / Sitemap / HTML / PDF
  ▼
جامع مقيد بالمصدر وrobots.txt
  │ استخراج نص + OCR عند الحاجة
  ▼
أرشيف غير قابل لفقد النسخ
  │ تصنيف + كيان + حدث + ادعاء + embedding
  ▼
SQLite FTS5 + vectors (نسخة واحدة)
  │
  ├── واجهة عربية وREST API
  ├── MCP tools/resources/prompts
  └── تصدير وبحوث محفوظة
```

## حدود الطبقات

- `collection.ts` و`ingestion.ts`: جلب عام محدود بالمضيف والسياسة والحجم والوقت.
- `pdf.ts`: تحويل PDF إلى نص وOCR؛ لا ينفذ محتوى المصدر.
- `knowledge.ts`: استخراج حتمي قابل للمراجعة، مع إثراء AI اختياري.
- `store.ts`: كتابة ذرية واستعلامات parameterized وحفظ الإصدارات.
- `mcp.ts` و`web.ts`: MCP وREST وواجهة الباحث فوق مخزن واحد.
- `views.ts`: عرض escaped فقط، ولا يعرض HTML خامًا من المصادر.

## البحث الهجين

1. FTS5 للنص العربي الموحّد.
2. cosine similarity لتمثيلات محلية أو Gemini اختيارية.
3. دمج الرتب بـ Reciprocal Rank Fusion حتى لا تلغي الدلالة دقة النص.
4. إرجاع سبب المطابقة والاستشهاد، لا درجة غامضة وحدها.

## مسار التوسع

- النسخة المنفردة: SQLite WAL، عامل جمع واحد، وواجهة قراءة متعددة العمال.
- النسخة الموزعة: PostgreSQL + pgvector، Redis للحد الموزع والمهام، وobject
  storage لملفات PDF. عقود النماذج والواجهات تظل ثابتة أثناء النقل.
